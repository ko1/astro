# pystro stub for `test` package — the CPython stdlib `test/` directory
# uses test.support / annotationlib / many CPython-internal modules.
# This stub keeps the package importable and provides minimal helpers
# so individual test_*.py files that only need basic skip/run wrappers
# can execute under pystro.
